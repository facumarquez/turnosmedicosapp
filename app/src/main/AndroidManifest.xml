<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.turnosapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Paciente_HomeActivity"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Usuario_verPerfil" />
        <activity android:name=".AltaTurno" />
        <activity android:name=".AltaDeTurno" />
        <activity android:name=".AgendaMedicoActivity" />
        <activity android:name=".AgendaMedicoFechaActivity" />
        <activity android:name=".AgendaMedicoHorarioActivity" />
        <activity android:name=".AgendaMedicoTurnoActivity"></activity>
    </application>

</manifest>